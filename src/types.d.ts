declare module 'leaflet';

// Add missing type definitions
interface CarbonEmissionSummary {
  net_carbon_balance?: number;
  // Add other missing properties
}

interface ScatterPoint {
  x: number;
  y: number;
}

// Add any other missing interfaces
